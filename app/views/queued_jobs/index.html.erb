<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
<div class="flash-container"></div>
<%= render '/partials/sidebar' %>
<div class="jobs">
  <% if @jobs.empty? %>
    <p class="empty-jobs-message">There are no new jobs at the moment. Check back later!</p>
  <% else %>
    <%= render partial: "/partials/jobs", locals: { partial: "queued_jobs_buttons" } %>
  <% end %>
</div>
