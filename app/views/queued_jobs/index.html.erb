<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
<div class="flash-container"></div>
<%= render '/partials/sidebar' %>
<div class="jobs">
  <% if @jobs.empty? %>
    <div class="empty-jobs-message">There are no more jobs in the queue. Check back for more later!</div>
  <% else %>
    <%= render partial: "/partials/jobs", locals: { partial: "queued_jobs_buttons" } %>
  <% end %>
</div>
